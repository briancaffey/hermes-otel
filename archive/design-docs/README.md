# Design notes (archive)

PRDs and plans that guided earlier releases (metrics, concurrency/export, config enrichment, the multi-backend dashboard, the refactor plan). Kept for history; the living documentation is the docs site under `website/docs/`.
