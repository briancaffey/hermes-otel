# Marketing material

Not part of the plugin or the docs site: the launch video project under `video/` (a HyperFrames composition, together with the instruction files its authoring tools read), the intro page (`intro.html`), a blog draft (`ARTICLE.md`) and the visual-identity brief (`DESIGN.md`). Excluded from lint and from the plugin security scan's artifact tree.
